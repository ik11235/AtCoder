package production.soundhound2018SummerQual.C

object Main extends App {

  import scala.math.pow

  var a = 2.0
  println(pow(a, 8.0))
  println(pow(a, -1.5))

  println(pow(pow(10.0, 9), pow(10.0, 9)))

  //  val Array(n, m, d) = scala.io.StdIn.readLine.split(" ").map(_.toInt)

}
